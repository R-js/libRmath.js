export interface connectionParameters {
    connectionData: {
        clientInstanceId; //ok
        performerId; //ok
        jsm2SessionId; //ok
        armaVersion; //ok
        host;
        onlyBusinessLogic;
        sessionID; //ok
        headers;
        url;
        ip;
        referer;
        agent;
    };
    clientData: {
        performerId.
    }; // benderClientData
    applicationId; //ok
    token;
    id;
    fmslp;
}
